name: '13 my build, corne with nice nano v2'
on: workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    secrets: inherit
    with:
      board: '["nice_nano"]'
      shield: '["settings_reset","corne_left","corne_right"]'
